/* const server = require("../../bin/www");
const io = require("socket.io").listen(server);

io.on("connection", sck => {
  const connectId = sck.id;
  sck.on("users:connect", () => {
    const user = {
      connectId: {}
    };
    console.log(111);
  });
}); */
